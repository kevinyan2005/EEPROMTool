<UserControl x:Class="OneWire.UI.Wpf.Views.OperationHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="900">
    <Grid>
        <DataGrid x:Name="HistoryDataGrid"
                  ItemsSource="{Binding History.Entries}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  AlternationCount="2"
                  Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp"
                                    Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                                    Width="160" />
                <DataGridTextColumn Header="Operation"
                                     Binding="{Binding Operation}"
                                     Width="180" />
                <DataGridTextColumn Header="Device"
                                     Binding="{Binding Device}"
                                     Width="*" />
                <DataGridTextColumn Header="Result"
                                     Binding="{Binding Result}"
                                     Width="220" />
                <DataGridTextColumn Header="Duration"
                                     Binding="{Binding Duration, StringFormat={}{0:hh\\:mm\\:ss\\.fff}}"
                                     Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
